{% extends 'base.html.twig' %}

{% block title %}Tag
{% endblock %}

{% block body %}
	<div class="wrapper">
		<h1>{{tag.name}}</h1>

		{{ include('photo/_cards.html.twig', { 'photos': tag.photos }) }}
	{# 
		<a href="{{ path('app_tag_index') }}">back to list</a> #}

		{# <a href="{{ path('app_tag_edit', {'id': tag.id}) }}">edit</a> #}

		{# {{ include('tag/_delete_form.html.twig') }} #}
	</div>
{% endblock %}
